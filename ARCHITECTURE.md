# معمارية النظام المحاسبي

## نظرة عامة
تم تصميم النظام باستخدام نمط معماري متقدم يوفر:
- فصل الاهتمامات (Separation of Concerns)
- إعادة الاستخدام (Reusability)
- سهولة الصيانة (Maintainability)
- قابلية الاختبار (Testability)

## الطبقات المعمارية

### 1. طبقة العرض (Presentation Layer)
- واجهات المستخدم (UI)
- التحكم في المدخلات
- عرض البيانات
- التفاعل مع المستخدم

### 2. طبقة الأعمال (Business Logic Layer)
- المنطق التطبيقي
- قواعد الأعمال
- معالجة البيانات
- التحقق من الصحة

### 3. طبقة الوصول للبيانات (Data Access Layer)
- الوصول لقاعدة البيانات
- عمليات CRUD
- استعلامات متقدمة
- التخزين المؤقت

### 4. طبقة النماذج (Models Layer)
- تمثيل الكائنات
- العلاقات بين الجداول
- التحقق من الصحة

## مكونات النظام الرئيسية

```
┌─────────────────────────────────────┐
│      واجهة المستخدم (UI)           │
├─────────────────────────────────────┤
│    خدمات الأعمال (Services)        │
├─────────────────────────────────────┤
│    مستودع البيانات (Repository)    │
├─────────────────────────────────────┤
│      قاعدة البيانات (Database)     │
└─────────────────────────────────────┘
```

## النماذج الرئيسية

### نموذج الحساب (Account)
```csharp
Accounts
├── Account ID
├── Account Number
├── Account Name
├── Account Type
├── Account Nature
├── Opening Balance
├── Current Balance
└── Status
```

### نموذج المعاملة (Transaction)
```csharp
Transactions
├── Transaction ID
├── Date
├── Description
├── Account From
├── Account To
├── Amount
├── Approved By
└── Status
```

### نموذج الفاتورة (Invoice)
```csharp
Invoices
├── Invoice ID
├── Invoice Number
├── Date
├── Customer
├── Items
├── Total Amount
├── Discount
├── Tax
└── Status
```

## تدفق البيانات

```
المستخدم
   ↓
[واجهة المستخدم]
   ↓
[خدمات الأعمال] → [التحقق من الصحة]
   ↓
[مستودع البيانات]
   ↓
[قاعدة البيانات]
```

## معايير التطوير
- استخدام async/await للعمليات غير المتزامنة
- تطبيق معالجة الأخطاء الشاملة
- كتابة اختبارات وحدة (Unit Tests)
- توثيق الأكواد بشكل دقيق
- اتباع معايير التسمية الموحدة
